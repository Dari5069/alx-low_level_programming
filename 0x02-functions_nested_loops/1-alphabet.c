#include "main.h"
/**
 * main - Entry point
 * description 'prints alphabet in lowercase
 *
 * return: 0 if success
 */
int main(void)
{
	int i;

	if (i >= 97 && i <= 122)
	{
		_putchar(i);
	}
	_putchar('\n');
		return (0);
}
