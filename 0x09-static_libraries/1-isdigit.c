#include "main.h"

/**
 *_isdigit - checks for a digit (0 through 9)
 * @c: The digit to be ckhecked
 * Return:return 1 if c is a digit character or 0 for anything else
 */
int _isdigit(int c)

{
return (c >= '0' && c <= '9');
}
